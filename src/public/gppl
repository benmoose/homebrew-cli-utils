# gppl list the repo's top contributors over the last year.
# See https://piechowski.io/post/git-commands-before-reading-code

emulate -L zsh
set -o pipefail

if ! _git_repo; then return 1; fi

git shortlog --since='1 year ago' --summary --merges --numbered -- "${*}"
